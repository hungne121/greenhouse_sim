<launch>
  <!-- Arguments -->
  <arg name="configuration_basename" default="greenhouse_slam.lua"/>
  <arg name="set_base_frame" default="base_footprint"/>
  <arg name="set_odom_frame" default="odom"/>
  <arg name="set_map_frame"  default="map"/>
  <arg name="open_rviz" default="true"/>
  <!-- Gmapping -->
  <node pkg="gmapping" type="slam_gmapping" name="greenhouse_gmapping" output="screen">
    <param name="base_frame" value="$(arg set_base_frame)"/>
    <param name="odom_frame" value="$(arg set_odom_frame)"/>
    <param name="map_frame"  value="$(arg set_map_frame)"/>
    <rosparam command="load" file="$(find greenhouse_sim)/param/gmapping_params.yaml" />
  </node>

  <!-- rviz -->
  <group if="$(arg open_rviz)"> 
    <node pkg="rviz" type="rviz" name="rviz" required="true"
          args="-d $(find greenhouse_sim)/config/greenhouse_gmapping.rviz"/>
  </group>
</launch>
